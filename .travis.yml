language: cpp

compiler:
  - gcc
  - clang
  
os:
  - linux
  
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq boost-all-dev
  
before_script:  
  - mkdir build
  - cd build
  - cmake ..
